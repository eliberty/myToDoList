import React from 'react';
import DetailedPostItem from '../components/DetailedPostItem';

const PostDetailed = () => {
  return (
    // TODO consider add condition on state.fetching : if true, display loader
    <DetailedPostItem />
  );
};

export default PostDetailed;
